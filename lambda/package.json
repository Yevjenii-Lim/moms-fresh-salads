{
  "name": "stripe-eventbridge-email-handler",
  "version": "1.0.0",
  "description": "Lambda function to handle Stripe EventBridge events and send emails",
  "main": "email-handler.js",
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "nodemailer": "^6.9.8"
  },
  "engines": {
    "node": "18.x"
  }
}
